alias ssh-vx ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no
alias scp-vx scp -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -r

#alias rmaxima rmaxima --init-mac='~/.maxima_startup' --init-lisp='~/.maxima_startup.lisp'

setenv EDITOR nvim

setenv XDG_CONFIG_HOME $HOME/.config
setenv XDG_CACHE_HOME $HOME/.cache
setenv XDG_DATA_HOME $HOME/.local/share

setenv PATH "${PATH}:${HOME}/.local/bin"
